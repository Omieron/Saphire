spring:
  application:
    name: Saphire

  # Database
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5345}/${DB_NAME:saphire_system}
    username: ${DB_USERNAME:saphire_master}
    password: ${DB_PASSWORD:saphire_password}
    driver-class-name: org.postgresql.Driver

  # JPA & Hibernate
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Flyway Database Migrations
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    baseline-version: 0
    locations: classpath:db/migration

  # Jackson (JSON)
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: ${TIMEZONE:Europe/Istanbul}

# Server
server:
  port: ${SERVER_PORT:8080}

# JWT Config
jwt:
  secret: ${JWT_SECRET:ee5ddc7600da0fc86d4bb16f5b3b4862c47c210bbb9f2c854e9b04cf9cfb6de3}
  expiration: ${JWT_EXPIRATION:28800000}

# Security
app:
  security:
    cors:
      allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}

# Logging
logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.crownbyte.Saphire: ${LOG_LEVEL_APP:INFO}
    org.hibernate.SQL: ${LOG_LEVEL_SQL:INFO}
    org.hibernate.type.descriptor.sql.BasicBinder: ${LOG_LEVEL_BINDER:INFO}